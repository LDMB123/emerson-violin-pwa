# Worktree Execution Plan

- `codex/tooling` at `.worktrees/tooling`
- Scope: instrumentation, docs, build budgets, ADRs

- `codex/offline` at `.worktrees/offline`
- Scope: service worker strategy, lesson packs, offline UI

- `codex/wasm-audio` at `.worktrees/wasm-audio`
- Scope: Rust audio engine, AudioWorklet/Worker boundary, tuner demo

- `codex/ui` at `.worktrees/ui`
- Scope: kid-first screens, touch targets, parent gate UX
