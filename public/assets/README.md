# Assets

- `audio/` instrument tones and metronome files
- `badges/` achievement badges
- `icons/` app icons and favicons
- `illustrations/` mascots and UI illustrations
- `mockups/` PWA screenshots referenced by `manifest.webmanifest`

Each asset category includes a short README describing its purpose.
