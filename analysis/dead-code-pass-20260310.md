# Dead-Code Pass Log (2026-03-10)

| file | signal source | rationale | risk | action | revert plan |
|---|---|---|---|---|---|
| `src/hooks/useMediaRecorder.js` | Manual verification after baseline scans | `stopRecording` accepted `_forcedDuration` arg that was never used; removing the parameter reduces API noise and callsite confusion without changing behavior. | Low. The function behavior did not reference the arg, and all existing callsites passed no meaningful value. | Updated `stopRecording` signature to `stopRecording = useCallback(async () => ...)`. | Re-add `_forcedDuration` to function signature and ignore it at call time if needed. |
| `src/hooks/useNativeSongPlayer.js` | Manual verification; follow-up scan for residual unused state | Removed dead local state scaffolding and placeholder values (`playheadPosRef`, `completed`, unused assignments) that were no longer referenced by any consumer. | Low. Notes are re-parsed each render and gameplay logic remains unchanged; only inert bookkeeping was removed. | Removed `playheadPosRef` ref, removed `completed: false` from note object, and dropped related writes. | Reintroduce the ref and values if future playhead resume/resync feature needs explicit position persistence. |
| `src/views/Songs/SongRunnerView.jsx` | Manual verification after `useNativeSongPlayer` simplification | Hook no longer exposes `status`/`currentNote` values, so destructuring them in caller became dead reads; passing unused `songId`/`waitMode` into hook was likewise unnecessary. | Low. UI behavior is unchanged; only dead reads/args removed. | Removed destructuring from hook call and removed `songId`, `waitMode` arguments passed into `useNativeSongPlayer`. | Revert to previous destructuring/arguments if the hook later grows runtime behavior for those values. |
| `src/views/Songs/SongRunnerView.jsx` | Manual verification after UI/hook alignment | `waitMode` checkbox state never influenced playback in this React implementation, and the checkbox itself offered no observable behavior. `useMemo` was unused. | Low. This removes inert UI and import noise while preserving recorder/song playback paths. | Removed `waitMode` state/handler/control and dropped `useMemo` from hook imports. | Re-add the control and state if wait-for-me behavior is reintroduced and wired to playback logic. |
| `src/ml/recommendations.test.js` | `scripts/find-orphaned-files.js` | Flagged as orphaned, but test files are intentionally imported through test tooling rather than runtime modules. | Low/none for runtime risk; do not delete. | Marked as **manual-review/keep**; no code change made. | If repository policy changes, move test files to an explicit non-orphan namespace and add runtime import references before deletion. |
| `src/utils/child-profile.test.js` | `scripts/find-orphaned-files.js` | Same as above; test fixture appears intentionally isolated for coverage only. | Low/none for runtime risk. | Marked as **manual-review/keep**; no code change made. | Remove only if test runner coverage strategy changes to explicit discovery. |
| `src/utils/daily-lesson-plan.test.js` | `scripts/find-orphaned-files.js` | Same as above; test file required for utility coverage. | Low/none for runtime risk. | Marked as **manual-review/keep**; no code change made. | Same as above. |
| `src/utils/practice-session.test.js` | `scripts/find-orphaned-files.js` | Same as above; test file required for utility coverage. | Low/none for runtime risk. | Marked as **manual-review/keep**; no code change made. | Same as above. |
| `public/robots.txt` | `scripts/find-orphaned-files.js` | Static public artifact, not imported like JS modules. | None; deleting would impact deploy metadata/crawling behavior. | Marked **no-action** due non-JS role. | Restore from git if deletion becomes necessary for deployment changes. |
| `analysis/20260310-dead-css.log` | `scripts/find-dead-css.js` | Tool reports 590 potentially unused CSS selectors, but list is too noisy due dynamic class generation and runtime DOM wiring. | Medium if blindly deleting; could impact live styling. | Kept as reference only; no deletions attempted. | Re-run script after larger UI refactor and manually validate selector usage before pruning. |
| `analysis/20260310-ghost-ids.log` | `scripts/find-ghost-ids.js` | 37 “ghost” IDs likely include dynamic/legacy UI paths and test-only references. | Medium for accidental UI breakage. | Kept as manual review bucket; no action. | Re-check against updated route/view contract and template renderer registry before deleting IDs. |
