# QA Scripts

- `qa-screenshots.mjs` captures QA screenshots into `docs/reports/qa/screenshots/`.
- `perf-report.js` converts exported perf JSON into a markdown report.
- `perf-bundle.js` bundles perf JSON, report, build meta, and optional screenshots.
