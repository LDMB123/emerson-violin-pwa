# Maintenance Scripts

- `cleanup-organization.sh` repo cleanup + archive helpers
- `prune-legacy.js` remove legacy coaching/teaching/games code + assets
- `token-budget.js` estimate in-scope token budget using `docs/ai/IGNORE.md`
- `stash-audit.js` summarize worktree changes
- `install-hooks.sh` install local git hooks
